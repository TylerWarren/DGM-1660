using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
      Console.WriteLine("Hello There Firends.");
      Console.WriteLine("How Are You?");
      int myNum = 10000;
      Console.WriteLine(myNum);
      int myNum2 = 5000;
      Console.WriteLine(myNum2);
      int sum1 = 20 + 20; // 40 (20 + 20)
     
    }
  }
}
